import './style.scss';

const NotFound = () => (
  <div className="container not-found">
    <h1 className="not-found-title">ERREUR 404</h1>
    <h2 className="not-found-title">Cette route n'existe pas ! Merci de retourner sur la piste cyclable !</h2>
  </div>
);

export default NotFound;
