import './style.scss';
import { Link } from 'react-router-dom';

const NotFound = () => (
  <div className="container not-found">
    <h1 className="not-found-title">Cette route n'existe pas ! Merci de retourner sur la piste cyclable</h1>
  </div>
);

export default NotFound;
